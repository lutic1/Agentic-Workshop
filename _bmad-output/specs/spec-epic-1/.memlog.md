---
topic: Epic 1: triage data and schema (triage-decision schema, seed loader into app.db)
updated: 2026-09-26T12:19
---

- (direction) input is INTENT.md (Epic 1 done-when/must-not-change/out-of-scope), read alongside seed/*.csv headers, mcp/triage_server.py queries, TRIAGE_POLICY.md and the Epic 2 spec that consumes Epic 1
- (decision) slug epic-1, matching sibling spec-epic-2 and spec-epic-3 folders
- (decision) input is rich; extracted directly, no elicitation
- (capability) CAP-1 triage-decision schema: a decision is a JSON object with category (billing|bug|access|performance|how-to), priority (P1-P4), route (billing-team|bug-team|access-team|performance-team|how-to-team) and a one-sentence rationale; anything else is rejected with a clear error; success: valid objects pass, and a bad enum value, a missing field or an extra field each fail with an error naming the field
- (capability) CAP-2 seed loader: uv run python load_seed.py loads seed/tickets.csv and seed/customers.csv into app.db as tables tickets and customers with the CSV columns; success: after the command, tickets has columns ticket_id,customer_id,created_at,text and customers has customer_id,name,plan,open_tickets, with row counts equal to the CSVs
- (capability) CAP-3 idempotent reload: running load_seed.py twice gives the same database; success: row counts and contents are identical after the first and second run, with no duplicate rows
- (constraint) Python 3.12 or newer, managed with uv
- (constraint) files in seed/ are read-only
- (constraint) no network calls and no API keys in this epic
- (constraint) mcp/triage_server.py already reads app.db (tickets: ticket_id,customer_id,created_at,text; customers: customer_id,name,plan,open_tickets); those table and column names must keep working
- (constraint) Epic 2 consumes the schema as the agent structured output and validator, and must not modify it; the schema must be importable from Python by Epic 2
- (note) non-goals from input: the agent, the MCP tools, evals, any user interface
- (assumption) Extra fields beyond the four are rejected, reading the input anything else is rejected strictly
- (assumption) app.db is written at the project root, where mcp/triage_server.py looks for it
- (question) Must the schema enforce the TRIAGE_POLICY.md category-to-route pairing (billing->billing-team etc.), or accept any valid category with any valid route?
- (question) How is one-sentence rationale enforced: reject empty only, or also reject multi-sentence text?
- (question) Should open_tickets be stored as INTEGER, or are all columns stored as text as read from the CSV?
- (event) Pass 1 coherence PASS: 3 capabilities each with intent and testable success, 5 constraints each ruling something out, 4 non-goals, concrete success signal, IDs CAP-1..3 unique; 2 assumptions and 3 open questions recorded
- (event) Pass 2 preservation PASS: every INTENT.md claim (schema fields/enums/clear error, loader command/tables/columns/idempotence, 4 must-not-change items, out-of-scope list) landed in SPEC.md; wrapper-only content: section headings Done when/Must not change/Out of scope
- (decision) story 1 Triage-decision schema covers CAP-1; runs first since Epic 2 depends on it; spec_checkpoint true, done_checkpoint true, no invoke_dev_with (user choice)
- (decision) story 2 Seed loader covers CAP-2 and CAP-3; spec_checkpoint true, done_checkpoint false, no invoke_dev_with (user choice)
- (event) stories.yaml validity check PASS: 2 entries with required fields, ids quoted and unique, prefix-free, no status field
