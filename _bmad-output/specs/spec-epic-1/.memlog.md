---
topic: Epic 1: triage data and schema
updated: 2026-09-26T12:27
---

- (direction) User requested /bmad-spec from INTENT.md with slug epic-1 on branch spec/kishore-epic-1.
- (decision) Epic 1 establishes the triage-decision contract and repeatable local seed data needed by later agent and eval epics.
- (capability) CAP-1: A triage decision is exactly a JSON object with category, priority, route, and one-sentence rationale; invalid decisions fail with a clear error.
- (decision) Allowed category values are billing, bug, access, performance, how-to; priorities are P1-P4; each category uses its same-named team route, consistent with TRIAGE_POLICY.md.
- (capability) CAP-2: uv run python load_seed.py loads seed/tickets.csv and seed/customers.csv into repo-root app.db tables tickets and customers; two runs leave the same database.
- (constraint) Python is 3.12 or newer and dependencies are managed with uv.
- (constraint) seed/ and TRIAGE_POLICY.md are read-only; app.db is local and must not be committed.
- (constraint) Epic 1 makes no network calls and uses no API keys.
- (constraint) Existing mcp/triage_server.py must continue reading repo-root app.db and the CSV-matching tickets and customers columns.
- (decision) Non-goals are the agent, MCP tool implementation, evals, and any user interface.
- (note) Source INTENT.md is fully absorbed; mcp/triage_server.py and seed CSV headers were consulted for the data contract.
- (assumption) Running the loader twice gives the same database means identical logical tables, columns, and rows, not necessarily byte-identical SQLite files.
- (decision) Keep field and route catalog in decision-schema.md, loader and MCP table contract in data-contract.md; adopt mcp/triage_server.py so downstream can verify existing queries.
- (constraint) customers.open_tickets must remain numeric for the existing MCP result and downstream Enterprise threshold rule; no other SQLite affinity is prescribed.
- (event) Self-Validate pass 1, coherence: PASS. CAP-1 and CAP-2 each have intent and testable success; constraints, non-goals, success signal, IDs, and lean prose satisfy Spec Law rules 1-6 and 8.
- (event) Self-Validate pass 2, preservation: PASS. Every load-bearing INTENT.md claim appears in SPEC.md or its companions; existing MCP table and column expectations are captured.
- (note) Wrapper-only content: INTENT.md section headings were omitted from the derived contract; no substantive source claim was dropped.
- (direction) User requested Story Breakdown for Epic 1 per cheat sheet: no spec or done checkpoints; schema story before loader story.
- (decision) Story 1, Validate triage decisions, covers CAP-1 and decision-schema.md; spec_checkpoint=false, done_checkpoint=false, no extra dev dispatch note.
- (decision) Story 2, Load local seed data, covers CAP-2 and data-contract.md; spec_checkpoint=false, done_checkpoint=false, no extra dev dispatch note.
- (event) Story Breakdown validation: PASS. stories.yaml parses as two ordered entries with quoted unique prefix-free IDs, required fields, no status, and both checkpoints false; no story-spec files pin IDs yet.
