---
topic: Epic 1: triage data and schema (decision schema + seed loader into app.db)
updated: 2026-09-26T12:13
---

- (direction) input is INTENT.md (Epic 1 done-when/must-not-change/out-of-scope), grounded against seed/*.csv headers, mcp/triage_server.py SQL, TRIAGE_POLICY.md categories/routes and the Epic 2/3 specs that consume Epic 1; slug epic-1 matches sibling spec-epic-2/3; express mode since input is rich
- (capability) CAP-1 triage decision schema: a decision is a JSON object with category (billing|bug|access|performance|how-to), priority (P1-P4), route (billing-team|bug-team|access-team|performance-team|how-to-team) and a one-sentence rationale; anything else is rejected with a clear error; success: a valid decision passes, and a bad category/priority/route, a missing field or an extra field each fail with an error naming the offending field
- (capability) CAP-2 seed loader: uv run python load_seed.py loads seed/tickets.csv and seed/customers.csv into app.db as tables tickets and customers with the same columns as the CSVs; success: after one run app.db has tickets (24 rows: ticket_id, customer_id, created_at, text) and customers (20 rows: customer_id, name, plan, open_tickets), and mcp/triage_server.py get_ticket/get_customer_history return rows from it
- (capability) CAP-3 idempotent reload: running the loader twice gives the same database; success: a second run leaves identical row counts and contents in both tables, no duplicates, no error
- (constraint) Python 3.12+, managed with uv; packages added with uv add
- (constraint) seed/ is read-only; the loader only reads it
- (constraint) no network calls and no API keys in Epic 1 -- schema and loader run fully offline
- (constraint) mcp/triage_server.py already queries app.db (tickets: ticket_id, customer_id, created_at, text; customers: customer_id, name, plan, open_tickets); those table and column names must keep working unchanged
- (constraint) allowed categories and routes are exactly those in TRIAGE_POLICY.md's table; the schema must not invent or drop any
- (constraint) Epic 2's agent returns this schema as structured output and Epic 3's valid_schema scorer validates against it, so it must be importable from Python as the single source of truth
- (constraint) app.db is git-ignored and never committed
- (note) non-goals: the agent, the MCP tools, evals, and any user interface (INTENT.md out of scope)
- (question) must route match category (billing->billing-team etc. per TRIAGE_POLICY.md), so billing + bug-team is rejected? INTENT only lists allowed values per field
- (question) how strictly is 'one-sentence rationale' enforced -- non-empty string only, or also a single-sentence check (e.g. one terminal period)?
- (question) should customers.open_tickets be stored as INTEGER (Enterprise rule compares against 3) or kept as text like the CSV?
- (assumption) 'same database' on reload means same tables, columns and row contents (replace, not append); byte-identical files not required
- (assumption) extra fields in a decision count as 'anything else' and are rejected
- (assumption) schema location/module name is left to the build story; Epic 2 imports it from wherever story 1 puts it
- (event) self-validate pass 1 (coherence): passed; 3 capabilities each with intent+success, intents kept to WHAT (module/storage choices left out), every constraint rules something out, 3 non-goals, success signal is a runnable demo, CAP ids fresh
- (event) self-validate pass 2 (preservation): passed; every INTENT.md done-when, must-not-change and out-of-scope claim traced to CAP-1..3, Constraints or Non-goals; INTENT.md fully absorbed, listed in sources; no wrapper-only content
- (decision) resolved open question: route must match category per TRIAGE_POLICY.md's one-to-one table; a mismatch (e.g. billing + bug-team) is rejected with a clear error -- folded into CAP-1 success
- (decision) resolved open question: customers.open_tickets is stored as INTEGER so the Enterprise rule's '3 or more' compares numerically; other columns stay text -- folded into CAP-2 success
- (direction) user requested Story Breakdown: two stories, no spec_checkpoint/done_checkpoint, no invoke_dev_with notes
- (decision) story 1 'Triage decision schema' covers CAP-1: allowed values per field, route-matches-category, one-sentence rationale, reject missing/extra fields with field-named errors
- (decision) story 2 'Seed loader' covers CAP-2 and CAP-3: load_seed.py loads seed CSVs into app.db tickets/customers with MCP-compatible columns (open_tickets INTEGER) and re-runs to the same database
- (event) stories.yaml validity check: passed; 2 entries, quoted unique ids '1' and '2', prefix-free, all required fields, no status field
- (event) re-run on INTENT.md with slug epic-1: source unchanged since create, no new claims; re-derive yields the same SPEC.md, so no rewrite; CAP ids and stories.yaml untouched
- (direction) Story Breakdown re-requested; user kept the existing two stories unchanged (no checkpoints, no invoke_dev_with); stories.yaml still matches SPEC.md, not rewritten
