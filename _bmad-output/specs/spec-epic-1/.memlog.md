---
topic: Epic 1: triage data and schema
updated: 2026-09-26T12:27
---

- (decision) Slug is epic-1, matching sibling spec-epic-2 and spec-epic-3.
- (capability) CAP-1: load_seed.py creates app.db with tickets and customers tables matching the seed CSVs, idempotently.
- (capability) CAP-2: triage decision is a JSON object with enumerated category/priority/route and a one-sentence rationale; anything else is rejected.
- (constraint) Python 3.12+ with uv; seed/ read-only; no network calls or API keys; mcp/triage_server.py table and column names must keep working.
- (decision) Non-goals: the agent, the MCP tools, evals, and any user interface.
- (event) Self-validate pass 1 (coherence): all 8 Spec Law rules satisfied; no assumptions or open questions.
- (event) Self-validate pass 2 (preservation): every load-bearing claim in INTENT.md landed in the kernel.
- (decision) Story 1: the loader (CAP-1) — load_seed.py reads seed CSVs into app.db idempotently.
- (decision) Story 2: the triage-decision schema (CAP-2) — validates category/priority/route/rationale, rejects malformed decisions.
- (event) Stories schema check: both entries parse, ids unique and prefix-free, no status field, ids quoted strings.
