---
topic: Epic 3: measure the triage agent with an MLflow eval, four code scorers, an LLM judge, and a token/escalation report
updated: 2026-09-25T00:52
---

- (direction) User gave a full done-when/must-not-change/out-of-scope brief for Epic 3 directly in the /bmad-spec invocation; rich input, distilled express (no elicitation needed).
- (capability) CAP-1: uv run python eval/run_eval.py runs the Epic 2 agent over all 20 tickets in eval/labelled_tickets.csv via mlflow.genai.evaluate, logging exactly one MLflow run to sqlite:///mlflow.db in the triage-agent experiment.
- (capability) CAP-2: valid_schema scorer checks each ticket's output against the Epic 1 triage-decision schema, scoring 1 when it validates, 0 otherwise.
- (capability) CAP-3: category_match scorer scores 1 when the output's category equals expected_category from eval/labelled_tickets.csv, 0 otherwise.
- (capability) CAP-4: priority_match scorer scores 1 when the output's priority equals expected_priority from eval/labelled_tickets.csv, 0 otherwise.
- (capability) CAP-5: tool_order scorer scores 1 when the ticket's MLflow trace shows get_ticket starting before get_customer_history, 0 otherwise.
- (capability) CAP-6: rationale_judge scorer asks a Groq model (ChatGroq, JUDGE_MODEL default openai/gpt-oss-120b, key GROQ_API_KEY) whether each ticket's rationale is sound given its judge_notes, returning pass or fail plus a one-line reason; never touches GEMINI_API_KEY.
- (capability) CAP-7: at the end of the run the script prints the mean of each of the five scorers and the agent's total tokens (read from the MLflow traces), and writes the same numbers to eval/latest_report.json.
- (capability) CAP-8: every escalation raised while evaluating the 20 tickets (e.g. T-1044, T-1048, T-1057) is approved automatically so the run never waits on a person, the agent's own escalation decision logic is untouched, and the count of auto-approved escalations appears in the printed output and in eval/latest_report.json.
- (constraint) The eval harness is built with mlflow.genai.evaluate, not a hand-rolled scoring loop.
- (constraint) Read-only, unchanged: eval/labelled_tickets.csv, TRIAGE_POLICY.md, and the Epic 2 agent's behaviour -- run_eval.py calls the existing agent as-is and never edits its decision logic, prompts, or policy handling.
- (constraint) rationale_judge always calls ChatGroq via JUDGE_MODEL/GROQ_API_KEY regardless of the agent's PROVIDER setting -- it never reads GEMINI_API_KEY, so it never competes for the agent's Gemini quota.
- (constraint) No network calls beyond model APIs: the four code scorers (valid_schema, category_match, priority_match, tool_order) run locally against schema/labels/trace data; only the agent's own model call and rationale_judge's Groq call cross the network.
- (constraint) eval/latest_report.json is the only new file this epic writes outside MLflow's own store; normal uv run python run_agent.py invocations still pause for a person's yes/no -- unattended approval applies only inside the eval run.
- (non-goal) Dashboards, CI, hosting, and tuning the agent to raise the score are out of scope, per the user's brief.
- (assumption) Epic 1 and Epic 2 are specced but not yet built on this branch; this spec references their specs (INTENT.md for the schema, spec-epic-2/SPEC.md for the agent's triage(ticket_id) entry point and MLflow setup) as the ground truth to build against, same as spec-epic-2 did for the not-yet-built Epic 1 schema.
- (assumption) The printed/report mean for rationale_judge treats pass=1, fail=0 and reports the pass rate, consistent with how the four 0/1 code scorers are averaged; the brief specified this at the WHAT level only ("mean of each scorer") without prescribing a scoring scale, so this is the natural reading.
- (decision) Companions: ../../../INTENT.md (Epic 1 schema, adopted), ../spec-epic-2/SPEC.md (agent entry point and MLflow setup, adopted), ../../../eval/labelled_tickets.csv (fixture columns, adopted), ../../../mcp/triage_server.py (tool names get_ticket/get_customer_history for tool_order, adopted). TRIAGE_POLICY.md not listed: judge_notes already encode the policy reasoning the judge checks against, so the judge does not need the policy text directly.
- (event) Self-validate pass 1 (coherence, rules 1-6,8): PASS. All 8 capabilities have intent+success. Intents are WHAT-level (rule 2); mlflow.genai.evaluate/ChatGroq/JUDGE_MODEL are given directly by the user as real constraints, not prescribed HOW. Each constraint rules something out. Non-goals present. Success signal is demonstrable via one command. CAP IDs unique/stable. Prose lean, no decoration found to cut.
- (event) Self-validate pass 2 (preservation): every done-when bullet lands in a CAP (CAP-1 harness, CAP-2..CAP-6 five scorers, CAP-7 report, CAP-8 escalation auto-approval+count); every must-not-change item lands in a constraint; every out-of-scope item lands in non-goals. No wrapper-only content dropped. Verdict: PASS.
- (event) SPEC.md rendered and presented to user: 8 capabilities, 4 companions, 3 assumptions, 0 open questions. Spec finalized for this run.
- (decision) User requested Story Breakdown, 2 stories, no spec_checkpoint/done_checkpoint/invoke_dev_with. Story 1 (CAP-1,2,3,4,5,8): the eval run over the 20 labelled tickets with the four code scorers and auto-approved escalations. Story 2 (CAP-6,7): the Groq rationale_judge scorer and the printed/eval/latest_report.json report.
- (event) stories.yaml rendered: 2 entries, ids '1'/'2' quoted and prefix-free, no status field, no spec_checkpoint/done_checkpoint/invoke_dev_with per user request. Checked against stories-schema.md validity rules 1-4: PASS.
- (constraint) Each ticket's prediction runs inside a single MLflow trace (e.g. mlflow.trace on the predict function) -- an approved escalation resumes the agent in a second call, which would otherwise autolog as a separate trace that CAP-5's tool_order scorer can't see.
- (event) Self-validate pass 1 (coherence, rules 1-6,8): PASS. New constraint rules out a real design choice (per-call autologged traces) that would silently break CAP-5's tool_order scorer; it doesn't touch capabilities, non-goals, or success signal, so those rules are unaffected. Prose lean.
- (event) Self-validate pass 2 (preservation): the user's new constraint claim lands verbatim in Constraints. No other content changed; nothing dropped. Verdict: PASS.
- (event) stories.yaml checked against the updated spec: Story 1 already covers CAP-1 (the eval harness) and CAP-5/CAP-8 (tool_order, escalation auto-approval); the new constraint tightens how Story 1 is built, adds no capability, so the story description still matches. Not re-run.
